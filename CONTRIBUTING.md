# Contributing to ContentOS

欢迎修复 bug、改进文档、补充匿名行为用例或实现公开适配器。

提交前请确认：改动服务于可观察问题；TaskKind 只在 canonical manifest 定义一次；profile、能力图、模块 Registry 和测试已同步；没有私人路径、身份、账号数据、凭据或运行数据库；PowerShell 入口不在 `param(...)` 默认表达式里依赖 `$PSScriptRoot`；行为与兼容影响已经测试和说明。

代码贡献按 MIT 许可；原创规则、模板和文档按 CC BY-SA 4.0 许可。提交即表示你有权按对应许可证贡献该内容。
